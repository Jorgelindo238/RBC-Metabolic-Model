# ================================================================
# RBC Metabolic Model - Python Dependencies
# ================================================================
# Compatible with Python 3.11+
# Supports both Streamlit Web App and CLI usage
# ================================================================

# Web / API runtime
streamlit>=1.28.0
fastapi>=0.115.0
uvicorn>=0.30.0
python-dotenv>=1.0.1
python-multipart>=0.0.9

# Core Scientific Computing
numpy>=1.24.0,<2.0.0
scipy>=1.10.0
pandas>=2.0.0

# Visualization
matplotlib>=3.7.0
plotly>=5.17.0

# Network Analysis (pathway visualization)
networkx>=3.1

# Excel File Support
openpyxl>=3.1.0
xlrd>=1.2.0

# Parameter Calibration (Bayesian optimization)
optuna>=3.0.0

# Authentication (Supabase)
supabase>=2.3.4

pytest>=7.4.0

# ================================================================
# Installation:
#   pip install -r requirements.txt
# ================================================================

# YAML Parsing
PyYAML>=6.0

# Agent Orchestration
langgraph>=0.0.26
langchain-core>=0.1.30
langsmith>=0.1.0

# API Configuration
pydantic>=2.0.0
pydantic-settings>=2.0.0

# OpenAI Integration
openai>=1.0.0

# ================================================================
# RoBoCop Agentic Supervisor (services/robocop/agentic/)
# ----------------------------------------------------------------
# These pins were previously in a separate requirements-agentic.txt
# and were folded back into this file on 2026-04-27. They are
# intentionally NOT installed by the production deployment manifests:
#   - api/requirements.txt
#   - apps/calibration-worker/requirements.txt
#   - apps/research-api/requirements.txt
#   - apps/monitoring-api/requirements.txt
# Those manifests are self-contained and do NOT pull from this root
# requirements.txt, so the agentic stack remains isolated from
# production runtime images. This root file is for local dev and the
# offline DeepAgents supervisor in services/robocop/agentic/ only.
# Per AgentOps/CalibrationOps.md ("DeepAgents direction"):
#   - prototype is offline-only
#   - all writes go through explicit bounded tools
#   - compare DeepAgents recommendations to the existing LangGraph
#     runner before any promotion
# ================================================================

# DeepAgents harness (LangChain/LangGraph-native agent harness)
# https://github.com/langchain-ai/deepagents
deepagents>=0.5.3

# Required by deepagents for model resolution and OpenAI tool calling.
# init_chat_model lives in langchain.chat_models, hence the umbrella.
langchain>=0.3.0
langchain-openai>=0.2.0
